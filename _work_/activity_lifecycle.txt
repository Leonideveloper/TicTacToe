class Activity:
	@static
	GameModel model;
	@static
	GameView view;

	def onRecreation():
		GameView oldView = view;
		view = GameViewFactory.createByOldView(oldView);
		connect(model, player_1, player_2, view);